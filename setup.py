"""
ANQ Scaff Setup - 企业级FastAPI项目脚手架生成工具

注意：此文件仅用于向后兼容。
所有配置已迁移到 pyproject.toml，推荐使用以下方式安装：
    pip install -e .
    或
    uv pip install -e .

setuptools 会自动从 pyproject.toml 读取配置。
"""

from setuptools import setup

# 使用 pyproject.toml 中的配置
# 此文件保留仅为兼容旧版工具
setup()
